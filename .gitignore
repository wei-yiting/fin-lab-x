.ruff_cache/
__pycache__/
*.pyc
.pytest_cache/
.venv
.env
.vscode/
.sisyphus/
.artifacts/
# artifacts/ — temporarily tracked during planning/implementation per CLAUDE.md feedback
# re-enable this exclusion once the design + plan + implementation are merged
.superpowers/
backend/evals/results/
node_modules/
.pnpm-store/
dist/
dist-ssr/
*.local
test-results/
playwright-report/

# SEC filing data
data/sec_filings_html/
data/sec_text/
data/sec_text_inspect/

# Logs
*.log
npm-debug.log*
pnpm-debug.log*

# data/ is tracked by default (curated assets like data/trace-archives/, see
# ADR-0005). When adding a NEW runtime writer under data/, list its output
# path here explicitly — reproducible runtime output never goes into git.
# Current runtime outputs: data/sec_filings_html/, data/sec_text/,
# data/sec_text_inspect/ (above), data/*.db (below).
*.db

# Editor / OS
.idea/
.obsidian/
.DS_Store
*.sw?
.claude/

# Understand Anything
.ua/
