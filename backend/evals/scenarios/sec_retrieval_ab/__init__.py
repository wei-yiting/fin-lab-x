"""SEC retrieval HTML vs text A/B scenario (sibling of sec_retrieval; assembled incrementally).

DEV-180 contributes only the frozen-HTML-arm compatibility module (html_arm_compat.py) —
the dataset, eval_spec.yaml, and scorer wiring land separately.
"""
