name: baseline_behavior_diagnostic
csv: dataset.csv
regression:
  # Diagnostic track, not the Regression Suite (CONTEXT.md: diagnoses
  # behavior and names the tuning lever, never grades quality) — gate
  # membership left to that track's own owners, not decided here.
  enabled: false
diagnostic:
  dataset_name: baseline_behavior_diagnostic
  dataset_version: "2026-09-03"
  agent_version: baseline
task:
  function: backend.evals.eval_tasks.run_baseline_behavior_diagnostic
column_mapping:
  question: input.question
scorers:
  - name: diagnostic_execution_health
    function: backend.evals.diagnostic.execution_scorer.execution_health
