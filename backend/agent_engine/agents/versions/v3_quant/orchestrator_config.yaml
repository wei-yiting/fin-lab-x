# =============================================================================
# NOTE: This configuration is illustrative / placeholder only.
# The tools, model settings, and constraints below are preliminary and subject
# to change once the v3_quant workflow is fully implemented and validated.
# =============================================================================

version: "0.3.0"
name: "v3_quant"
description: "Numerical reasoning, data visualization, and quantitative modeling"

tools:
  - yfinance_stock_quote
  - yfinance_get_available_fields
  - tavily_financial_search
  - sec_official_docs_retriever
  - duckdb_query
  - text_to_sql

model:
  name: "gpt-4o"
  temperature: 0.0

constraints:
  max_tool_calls_per_step: 8
