# Matrix row 5 — OpenAI Responses API reasoning ON (gpt-5 series).
# reasoning_effort=medium and use_responses_api=true are applied by the orchestrator when reasoning="on".
model:
  name: "openai:gpt-5-mini"
  temperature: 0.0
  reasoning: "on"
  thinking_budget: null
