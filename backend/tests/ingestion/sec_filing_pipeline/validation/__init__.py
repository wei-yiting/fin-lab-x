"""SEC HTML preprocessor validation harness.

CLI orchestrator + helpers used by BDD verification to run the
HTMLPreprocessor against real EDGAR 10-K filings and emit
per-round structural reports.
"""
