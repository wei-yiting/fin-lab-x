version: "0.2.0"
name: "reader"
description: "Structured RAG over SEC 10-K filings (Qdrant dense retrieval) with chunk-level citations"

tools:
  - finnhub_stock_quote
  - finnhub_company_basic_financials
  - tavily_financial_search
  - sec_filing_list_sections
  - sec_filing_get_section
  - sec_filing_search

model:
  name: "openai:gpt-4o-mini"
  temperature: 0.0
  reasoning: "unsupported"
  thinking_budget: null

constraints:
  max_tool_calls_per_run: 10
