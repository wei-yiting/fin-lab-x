# Artifacts are planning / design / BDD scaffolding kept on disk during
# implementation but NOT shipped in the PR (per feedback_no_commit_artifacts).
# Re-track selectively for the next planning cycle by removing the wildcard
# or adding explicit allowlist entries.
*
!.gitignore
