"""Cross-subsystem domain types shared by agent_engine and ingestion.

Import from submodules directly (e.g. ``from backend.common.sec_core import
FilingType``); this package intentionally does not re-export to keep the
import surface explicit.
"""
